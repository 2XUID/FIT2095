import { AgeComparePipe } from './age-compare.pipe';

describe('AgeComparePipe', () => {
  it('create an instance', () => {
    const pipe = new AgeComparePipe();
    expect(pipe).toBeTruthy();
  });
});
